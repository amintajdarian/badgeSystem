import 'package:flutter/material.dart';

LinearGradient diffaultGradient = const LinearGradient(
    colors: [Color(0xff222564), Color(0xff060134)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter);

LinearGradient greenGradient = const LinearGradient(
    colors: [Color(0xff00b306), Color(0xff008404)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter);

LinearGradient redGradient = const LinearGradient(
    colors: [Color(0xFFF74A4A), Color(0xffC20000)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter);
LinearGradient infoRedGradient = const LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    stops: [0, .85],
    colors: [Color(0xffFE3400), Color(0xff982709)]);
LinearGradient infoGreenGradient = const LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    stops: [0, .85],
    colors: [Color(0xff33D537), Color(0xff035105)]);
LinearGradient infoYellowGradient = const LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    stops: [0, .85],
    colors: [Color(0xffFEB200), Color(0xffC54A00)]);
LinearGradient infoBlueGradient = const LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    stops: [0, .85],
    colors: [Color(0xff4A9FFF), Color(0xff093D7A)]);
LinearGradient disableGradient = const LinearGradient(
    colors: [Color(0xFF727272), Color(0xff444444)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter);

LinearGradient yellowGradient = const LinearGradient(
    colors: [Color(0xffFFB303), Color(0xffAC7A07)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter);
